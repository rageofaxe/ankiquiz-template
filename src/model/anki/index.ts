/* DRAFT */
import { createEvent } from "effector";

// Effector:  events
export const _next = createEvent();
export const easy = createEvent();
export const normal = createEvent();
export const hard = createEvent();
